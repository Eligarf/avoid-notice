<div class="dice-roll">
  <div class="dice-result">
    <h4 class="dice-total">{{stealth}}</h4>
  </div>
</div>
<div>
  {{#each undetected as |target|}}
  <b>{{target.name}}</b> DC{{target.dc}}: <span class="{{target.resultClass}}"><b>{{target.result}}</b></span> <span data-visibility="gm" data-whose="opposer">{{target.delta}}</span>.<br>
  {{/each}}
</div>